# BULLETPROOF REQUIREMENTS FOR RUNPOD A40 (CUDA 12.4)
# These exact versions work together without conflicts

# Core PyTorch (CUDA 12.4 compatible - RunPod default)
torch==2.4.0
torchaudio==2.4.0
torchvision==0.19.0

# Critical: NumPy version MUST be <2.0.0 for compatibility
numpy==1.26.4

# Voxtral Requirements (EXACT VERSIONS)
transformers==4.54.0
mistral-common[audio]==1.8.2
accelerate==0.33.0

# vLLM for Voxtral (CRITICAL - exact version)
vllm==0.6.3.post1

# Orpheus TTS (EXACT VERSION - newer versions break)
orpheus-speech==0.1.0

# FastAPI and WebSocket
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==12.0
python-multipart==0.0.9

# Audio Processing (TESTED VERSIONS)
librosa==0.10.2
soundfile==0.12.1
scipy==1.11.4

# Additional Requirements
pydantic==2.5.3
python-dotenv==1.0.0
aiofiles==23.2.1

# System Monitoring
psutil==5.9.6
GPUtil==1.4.0

# Production Dependencies
gunicorn==21.2.0
httpx==0.25.2

# Logging
structlog==23.2.0
